# 노트 저장 규칙 (sallysung78 개인 노트 저장소)

이 저장소는 sallysung78 님이 Claude와 질문/답변하며 만든 노트(.md)를
**한 곳에 모아서** 여러 기기(맥북·아이폰)에서 관리하기 위한 공간이다.

## 핵심 규칙 — 항상 지킬 것

1. **모든 노트는 `notes/` 폴더 안에만 저장한다.** 루트에 흩어놓지 않는다.
2. `notes/` 아래에 **주제별 폴더**로 분류한다. (예: `notes/요리/`, `notes/공부/`, `notes/여행/`)
   - 알맞은 주제 폴더가 없으면 새로 만든다.
3. 파일명은 **한글 제목 + 하이픈(`-`)** 형식. (예: `뵈프-부르기뇽.md`)
4. 노트를 추가/수정하면 **항상 `notes/README.md` 목록(인덱스)을 함께 갱신**한다.
5. 작업 후 **커밋하고 푸시한다.** 사용자가 다른 기기에서 바로 받아볼 수 있어야 한다.

## 기기 간 작업 방식

- 사용자는 맥북(VSCode)과 아이폰(Claude 앱)을 오가며 작업한다.
- GitHub 저장소가 유일한 "원본(source of truth)"이다. 결과물은 항상 여기에 모은다.
- 가능하면 노트 작업은 **한 브랜치**로 통일해 흩어지지 않게 한다.

## 참고

- 저장소 루트의 `litellm/`, `nginx/`, `scripts/`, `docker-compose.yml` 등은
  별개의 인프라 설정 파일이다. 노트와 섞지 않는다.
